#include <stdio.h>

int convrt(char* fArr[], int* plen) {
    int i;
    char* fArr(plen);
    fArr[*plen] = i[plen];
    return i;
}

int main() {
    char* fArr = {"7", "1", "14", "-5"};
    int iArr = convrt(*fArr, 4);
    printf("%d", iArr);
    return 0;
}